# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
tfplan
*.tfplan

# Credentials — never commit these
*.pem
*.key
terraform.tfvars.local
terraform.tfvars
bootstrap/terraform.tfvars
